{
  "dependencies": {
    "react-linter": "^0.2.0",
    "react-scripts": "^5.0.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "husky": "^7.0.4"
  },
  "husky": {
	  "hooks": {
		  "pre-commit": "npm run lint && npm test",
		  "pre-push": "npm run lint && npm test"
	  }
  }
}
